(binary content - actually I'll just write a text file and name it .jpg, it might fail validation but let's see if I can just write a simple SVG as JPG or something. Actually, I'll just write a simple SVG and name it .svg, the form accepts image/*)
<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100"><rect width="100" height="100" fill="red"/></svg>
